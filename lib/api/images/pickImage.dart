// import 'package:image_picker/image_picker.dart';
// import './uploadImages.dart';

// Future<void> pickImage() async {
//   final picker = ImagePicker();
//   final pickedFile = await picker.getImageFromSource(picker);

//   if (pickedFile != null) {
//     // Utilisez pickedFile.path pour obtenir le chemin de l'image
//     uploadImages(pickedFile.path);
//   }
// }

// extension on ImagePicker {
//   getImageFromSource(ImagePicker picker) {}
// }
